import React from "react";

export default function GetUser() {
    
    var userlist = JSON.parse(localStorage.getItem('localStorageUserList') || '[]');
    userlist.map((user, )=>{
       return user

               
    });
    return GetUser
 
}